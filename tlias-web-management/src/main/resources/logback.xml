<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 定义日志文件存放目录 -->
    <property name="LOG_PATH" value="log" />

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 文件输出：所有日志，每个文件最大 10MB，按天归档 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/tlias.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/tlias.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- 每个文件最大 10MB -->
            <maxFileSize>10MB</maxFileSize>
            <!-- 保留最近 30 天 -->
            <maxHistory>30</maxHistory>
            <!-- 总日志文件大小上限 -->
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 错误日志单独输出，每个文件最大 10MB，按天归档 -->
    <appender name="FILE-ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/tlias-error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/tlias-error.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- 每个文件最大 10MB -->
            <maxFileSize>10MB</maxFileSize>
            <!-- 保留最近 30 天 -->
            <maxHistory>30</maxHistory>
            <!-- 总日志文件大小上限 -->
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 根日志级别 -->
    <root level="OFF">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
        <appender-ref ref="FILE-ERROR" />
    </root>

    <!-- 项目包日志级别（可调整为 DEBUG 查看详细 SQL） -->
    <logger name="com.lcx" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
        <appender-ref ref="FILE-ERROR" />
    </logger>
</configuration>
